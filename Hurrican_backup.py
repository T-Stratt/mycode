#!/usr/bin/env python3

# Input what the weather is.
weather = input("What is the weather in your area today?")

# Takes and saves the answer as a variable.
answer = input("What is the weather in your area today?")

#
temperature = input() 

# Takes the answer and is inputed into the output of message.
message = print(f"Today the weather is {answer}. We recommend")

# if input value is then will append to message.
if answer = Sunny and temperature >=
    message = message + '.'
elif connection >= 5:
    message = message + 'setting video to 1080p.'
elif connection >= 2:
    message = message + 'setting video to 720p.'
else:
    message = message + 'finding another access network.'
print(message)

